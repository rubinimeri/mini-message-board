<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <% for (let i = 0; i < messages.length; i++) { %>
        <div class="message">
            <h3><%= messages[i].username %></h3>
            <p><%= messages[i].message %></p>
            <a href="/<%= messages[i].id %>">Open</a>
        </div>
    <% } %>
    <a href="/new">Add message</a>
</body>
</html>